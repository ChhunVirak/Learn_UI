import 'package:flutter/material.dart';

class DetailPhone extends StatelessWidget {
  const DetailPhone({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Icon(
          Icons.home,
          size: 50,
        ),
      ),
    );
  }
}
